<div class="center">
  <h2>건강 수첩</h2>
  <h4>멍냥 의료정보에 대해서 기록해주세요.</h4>
</div>
<% if current_user.pets.empty? %>
  <div class="center" style="margin: 6rem;">
    <h1>건강 수첩은 멍냥 프로필을 작성 후 사용하실 수 있습니다</h1>
    <a href="/profile/create" class="btn">작성하러 가기</a>
  </div>
<% else %>
  <div class="row">
    <% @my_pets.each do |item| %>
      <a href="/healthnote/note_detail/<%= item.id %>">
        <div class="col s12 m6">
          <div class="card" style="margin: 2rem;">
            <div class="card-image">
              <img src="<%= item.img.url %>">
              <span class="card-title" style="background-color: rgba(0,0,0,0.8); margin: 1rem; border-radius: 1em; padding: 0.3rem 2rem;"><%= item.name %></span>
            </div>
            <div class="card-content black-text">
              <p><%= item.about %></p>
            </div>
          </div>
        </div>
      </a>
    <% end %>
  </div>
<% end %>

